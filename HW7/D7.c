/**
 * @author Перевозчиков Даниил
 * --------------------------------------
 * @details - D7 - Составить рекурсивную функцию, печать всех чисел от N до 1
 * --------------------------------------
 */

#include "stdio.h"

void rec(int a)
{
    if (a > 0) {
        printf("%d ", a);
        rec(a-1);
    }
}

int main()
{
    int a = 0;
    scanf("%d", &a);
    rec(a);
    return 0;
}