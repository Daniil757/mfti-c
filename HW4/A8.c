/**
 * @author Перевозчиков Даниил
 *
 * --------------------------------------
 * | Ввести три числа и найти наибольшее из них  |
 * --------------------------------------
*/

#include "stdio.h"

int main()
{
    int a, b, c, res;
    scanf("%d %d %d", &a, &b, &c);

    res = a > b ? a : b;
    res = res > c ? res : c;
    printf("%d",res);
    return 0;
}